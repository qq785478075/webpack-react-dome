import React, { Component } from 'react';
import './App.less'

export default class App extends Component {
    render() {
        return (
            <div className="app">
                <a href="/">Hello React!</a>
            </div>
        );
    }
}